# @package _global_

coco_train:
  name: coco_train
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.coco_train_root}
    annotations: ${data.coco_annotations_root}/person_keypoints_val2017.json
  support:
    root: ${data.coco_train_root}
    annotations: ${data.coco_annotations_root}/person_keypoints_val2017.json
  shuffle: true
  draw: false

coco_val:
  name: coco_val
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.val_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.coco_val_root}
    annotations: ${data.coco_annotations_root}/person_keypoints_val2017.json
  support:
    root: ${data.coco_val_root}
    annotations: ${data.coco_annotations_root}/person_keypoints_val2017.json
  shuffle: false
  draw: false

fsod_train:
  name: fsod_train
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.fsod_root}
    annotations: ${data.fsod_annotations_root}/fsod_train.json
  support:
    root: ${data.fsod_root}
    annotations: ${data.fsod_annotations_root}/fsod_train.json
  shuffle: true
  draw: false

fsod_val:
  name: fsod_val
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.fsod_root}
    annotations: ${data.fsod_annotations_root}/fsod_test.json
  support:
    root: ${data.fsod_root}
    annotations: ${data.fsod_annotations_root}/fsod_test.json
  shuffle: true
  draw: false

megapixel_mnist_train:
  name: megapixel_mnist_train
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_train_root}
    annotations: ${data.megapixel_mnist_annotations_root}/train.json
  support:
    root: ${data.megapixel_mnist_train_root}
    annotations: ${data.megapixel_mnist_annotations_root}/train.json
  shuffle: true
  draw: false

megapixel_mnist_val_novel_cats:
  name: megapixel_mnist_val_novel_cats
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_val_novel_cats_root}
    annotations: ${data.megapixel_mnist_annotations_root}/val_novel_cats.json
  support:
    root: ${data.megapixel_mnist_val_novel_cats_root}
    annotations: ${data.megapixel_mnist_annotations_root}/val_novel_cats.json
  shuffle: true
  draw: true

megapixel_mnist_val_train_cats:
  name: megapixel_mnist_val_train_cats
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_val_train_cats_root}
    annotations: ${data.megapixel_mnist_annotations_root}/val_train_cats.json
  support:
    root: ${data.megapixel_mnist_val_train_cats_root}
    annotations: ${data.megapixel_mnist_annotations_root}/val_train_cats.json
  shuffle: true
  draw: true

megapixel_mnist_grided_train:
  name: megapixel_mnist_grided_train
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_grided_train_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/train.json
  support:
    root: ${data.megapixel_mnist_grided_train_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/train.json
  shuffle: true
  draw: false

megapixel_mnist_grided_val_novel_cats:
  name: megapixel_mnist_grided_val_novel_cats
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_grided_val_novel_cats_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/val_novel_cats.json
  support:
    root: ${data.megapixel_mnist_grided_val_novel_cats_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/val_novel_cats.json
  shuffle: true
  draw: true

megapixel_mnist_grided_val_train_cats:
  name: megapixel_mnist_grided_val_train_cats
  bs: ${bs}
  k_shot: ${k_shot}
  bbox_format: ${data.bbox_format}
  input_size: ${data.input_size}
  transforms: ${data.train_transforms}
  backbone_stride: ${model.backbone.stride}
  query:
    root: ${data.megapixel_mnist_grided_val_train_cats_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/val_train_cats.json
  support:
    root: ${data.megapixel_mnist_grided_val_train_cats_root}
    annotations: ${data.megapixel_mnist_grided_annotations_root}/val_train_cats.json
  shuffle: true
  draw: true
